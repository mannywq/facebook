<%= turbo_frame_tag "#{dom_id(post, :comments)}" do %>

  <%= content_tag :button, data: { action: "toggle#comments", toggle_element_param: "#{post.id}-comment-field" } do %>
    <span class="text-gray-800 text-sm font-semibold">Comment</span>
  <% end %>

<% end %>
