<!DOCTYPE html>
<html>
  <head>
    <title><%= yield :title %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="prose min-w-full h-screen flex flex-col">
    <header class="w-full h-12 p-4 bg-white text-blue-700 flex p-2">
      <h2 class="font-semibold m-0">Incel</h2>
    </header>
    <main class="px-5 pb-12 grid grid-cols-2 w-full h-screen">
      <div class="grid place-items-center bg-cover" style="background-image: url(<%= asset_path 'register-gate.jpg' %>)">
              </div>
              <div>
      <div class="auth ml-[100px] max-w-[400px] p-12"><%= yield %></div></div>
      <div class="basis-0 hidden"></div>
    </main>
  </body>
</html>
