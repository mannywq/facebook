<%= button_to('Add friend',
  friends_path,
    { method: :post,
remote: true,
class: "rounded-lg font-bold text-white p-2 bg-blue-900 text-sm", params: { user: { friend: @user, status: 'pending' } } } ) %>
